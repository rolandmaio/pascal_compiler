#ifndef OPCODE
#define OPCODE

enum Opcode{

    WRITE_INT,                  // 0
    WRITE_REAL,
    WRITE_STRING,
    WRITE_BOOLEAN,
    WRITE_LINE,                 // 4

    PUSH_STRING,
    PUSH_INT,
    PUSH_REAL,
    PUSH_BOOLEAN,
    PUSH_ADDRESS,
    SWAP,

    PUSH_STRING_VAR,
    PUSH_BOOLEAN_VAR,

    POP_STRING,
    POP_INTEGER,
    POP_REAL,
    POP_BOOLEAN,

    INTEGER_ADDITION,
    INTEGER_MULTIPLICATION,
    INTEGER_DIVISION,
    INTEGER_MOD_DIVISION,
    INTEGER_SUBTRACTION,
    INTEGER_SUB_ONE,
    INTEGER_ADD_ONE,
    REAL_ADDITION,
    REAL_MULTIPLICATION,
    REAL_DIVISION,
    REAL_SUBTRACTION,

    EQ_BOOLEAN,
    NEQ_BOOLEAN,
    EQ_INTEGER,
    NEQ_INTEGER,
    LT_INTEGER,
    GT_INTEGER,
    LEQ_INTEGER,
    GEQ_INTEGER,

    CONVERT_INTEGER_TO_REAL,

    JUMP_FALSE,
    JUMP,

    TERMINATE_PROGRAM

};

#endif
